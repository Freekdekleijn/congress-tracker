# Wijzig de branch naam naar main
git branch -m main

# Voeg je GitHub repository toe (vervang met jouw username/repo)
git remote add origin https://github.com/Freekdekleijn/congress-tracker.git

# Commit alles
git commit -m "Initial commit: Congress Trading Tracker app"

# Push naar GitHub
git push -u origin main
# Ga naar je project folder in je terminal
cd /pad/naar/je/project

# Check of beide workflow files er zijn
ls -la .github/workflows/

# Als build-apk.yml er is, voeg het toe en push
git add .github/workflows/build-apk.yml
git commit -m "Add APK build workflow"
git push origin main
